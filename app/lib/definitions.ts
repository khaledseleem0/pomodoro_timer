export type Task = {
    id:string,
    createdAt:string,
    title:string
}
export type sessionsTableDataType = {
    _id:string,
    duration:number,
    breakDuration:number,
    createdAt:string,
}